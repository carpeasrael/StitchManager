# Gate-Protokoll 20260827-100539

| Feld | Wert |
|---|---|
| Ergebnis | **FREIGEGEBEN** |
| Tier | commit |
| Prüfumfang | gestagete Änderungen (Index) |
| Geprüfter Tree | `58be9d1ad779d9cf8fd9cc0118c20c6d7a3ec447` |
| Gate-Version | 1.0.0 |
| Gate-Signatur | `c506f60d323fe6d7b89f7b8b14a65136f4c6bc19` |
| Branch | main |
| Diff-Größe | 89889 Byte |
| Commit | `3ef4b28ad1e026b9455a5b2396cce4858f8eb4bb` |

## Stufen

| Stufe | Ergebnis | Detail |
|---|---|---|
| 0.1 Secret-Scan | PASS |  |
| 0.2 Diff-Kappung | PASS | 89889 von 400000 Byte |
| 0.3 Injection-Vorfilter | PASS |  |
| 0.4 Binärdateien | PASS | keine |
| 0b Selbsttest | PASS |   ✓ X2 dasselbe in einer .qml ist eines  Selbsttest: 157 bestanden, 0 fehlgeschlagen |
| 0b Selbsttest Projektregeln | PASS |  Selbsttest Projektregeln: 61 bestanden, 0 fehlgeschlagen |
| 0b Selbsttest Planprüfungen | PASS |  Planprüfungen-Selbsttest: 26 bestanden, 0 fehlgeschlagen |
| 0b Selbsttest QML-Prüfung | PASS |  Selbsttest QML: 26 bestanden, 0 fehlgeschlagen |
| 0c Dokumentprüfungen | PASS | aus dem Cache (Signatur 8d5cc7d6abc39f83f125db82d18e4fa0146b87ed) |
| 0c Projektregeln | PASS | aus dem Cache (Signatur a21ac2ff2b00e5ed6f9f0851e4bbc9f9172ed9be) |
| 0c cargo fmt | PASS | aus dem Cache (Signatur bebed28bbd48a0fefe264cbb603ceb6f3ab4bd8a) |
| 0c cargo clippy | PASS | aus dem Cache (Signatur 23fb05e498dec6d675be5b9ddd4bb38ec17de442) |
| 0c cargo test (volle Suite — Wurzelkonfiguration berührt oder Auswahl nicht auflösbar) | PASS | aus dem Cache (Signatur 42e39702b1605cefb0fa3bb1ea1a1c685ee42ac8) |
| 0c cargo deny | PASS | aus dem Cache (Signatur 26639abaff120d56f398f8b72f884d71ebd62c95) |
| 0c QML | PASS | aus dem Cache (Signatur f4e3566c820f906935144127c1b17f9f73d30121) |
| 1 Vier-Augen-Konsens | PASS | 4/4 APPROVE |
| 2 Schwere Gates | ENTFÄLLT | commit-Tier — laufen im pre-push-Hook |

## Stufe 1 — Vier-Augen-Konsens (4/4)

| Reviewer | Votum | Herkunft |
|---|---|---|
| **newton** | APPROVE | frisch (Versuch 1) |
| **turing** | APPROVE | frisch (Versuch 1) |
| **tesla** | APPROVE | frisch (Versuch 1) |
| **curie** | APPROVE | frisch (Versuch 1) |

### newton

> ## Prüfbericht Newton — Performance (commit-Tier)
> 
> **Gegenstand des Änderungssatzes:** `package.json`, `package-lock.json` (neu), `.gitignore` (Ergänzung Fuzzing-Pfade), ein Analysedokument. **Kein Code auf einem heißen Pfad** — keine Schleife, keine Abfrage, kein Zeichenpfad, keine Kernschnittstelle. Die Messlatten SM-LIB-009, SM-SRC-007, SM-PRV-007, SM-NFR-001/002/003/004 werden von diesem Diff weder berührt noch verschlechtert.
> 
> Geprüft und ohne Befund:
> 
> - `.gitignore:54-56` — `**/fuzz/target|corpus|coverage/` **entlasten** den je Commit laufenden Pfad: `scripts/lib/dateien.sh` bildet seine Liste über `git ls-files --cached --others --exclude-standard`; ein ungefiltertes libFuzzer-Korpus wächst unbegrenzt und ginge sonst in jeden Lauf von `check-docs.sh`, `check-projektregeln.sh`, `check-qml.sh` und in den Binärabschnitt der Stufe 0 ein. Die Wirkung ist O(Korpusgröße) je Commit und wird hier auf 0 gesetzt. `fuzz-saat/` wird vom Muster korrekt nicht getroffen (Pfadkomponente `fuzz-saat` ≠ `fuzz`).
> - `fuzz/artifacts/` bleibt sichtbar: Die dort entstehenden Dateien tragen keine der Erweiterungen aus `KN_QUELL_EXT`/`sources()` und schlagen daher nicht in den Stil- und Regelprüfungen auf; die Zusatzlast je Gate-Lauf ist eine Handvoll `stat`-Aufrufe. Kein messbarer Befund.
> - `package.json:11` — Prüfbereich nicht mehr im Skriptaufruf, sondern allein in `.markdownlint-cli2.jsonc` (`globs`/`ignores`). Deckungsgleich mit `scripts/check-docs.sh:344-348`; `node_modules/**` und `.git/**` werden von fast-glob als Verzeichnis geschnitten, keine Volltraversierung.
> - SM-PLT-007: `package.json` Version `0.1.0` = `Cargo.toml:25` `workspace.package.version = "0.1.0"`.
> - Leistungsaussagen im Analysedokument sind belegt und korrekt eingestuft: Der Lauf mit `SM_PRUEFBESTAND_ANZAHL=200` ist ausdrücklich als **Kurzprüfung** (Ausgang 4) und nicht als Abnahme ausgewiesen, die Bezugsmenge 100.000 (SM-LIB-009, SM-NFR-001, AK-01) genannt, SM-NFR-004 als nicht nachgewiesen benannt, kein Erstnachweis in der Matrix. Das ist genau die von Abschnitt 13.2 des Lastenhefts verlangte Trennung; keine geschätzte Zahl als Messwert ausgegeben.
> 
> ### Befunde
> 
> **1 — minor (Vermutung, nicht verifiziert)**
> 
> - **Ort:** `package-lock.json:~430` (Eintrag `node_modules/markdownlint-cli2`)
> - **Kennung:** SM-OSS-011, `CLAUDE.md` Abschnitt 11 („Werkzeugfassung gebunden … als Entwicklungsabhängigkeit")
> - **Fehlerbild:** Der Knoten trägt `"dev": true` **nicht**, sondern nur `"peer": true` (Peer-Kante aus `markdownlint-cli2-formatter-default`). Der dokumentierte Bezugsweg `npm ci` ist davon nicht betroffen — er installiert das Werkzeug. Betroffen wären nur Aufrufe mit `--omit=peer` (Werkzeug fehlt → `check-docs.sh:367` meldet FAIL) bzw. `--omit=dev` (Werkzeug wird entgegen der Absicht mitinstalliert). Wirkung im dokumentierten Ablauf: keine.
> - **Vorschlag:** `markdownlint-cli2-formatter-default` zusätzlich als `devDependency` führen, damit die Wurzelkante `dev` trägt — oder den Sachverhalt in `package.json` als Kommentarzeile der Analyse festhalten, damit er beim nächsten Sperrdateilauf nicht als Regression gelesen wird.
> 
> **2 — Beobachtung ohne Blockerwirkung (S4)**
> 
> - **Ort:** `Analysis/20260826_02_bestandsaufnahme.md:—` (Abschnitt 9, Nachweistabelle)
> - **Kennung:** SM-NFR-004, SM-NFR-002, AK-01, OP-08
> - **Fehlerbild:** Für SM-NFR-004 (fünf Sekunden bis bedienbereit bei 100.000 Einträgen) liegt im Baum kein Nachweis vor, und SM-NFR-002 ist bislang nur bei 200 Einträgen belegt — bei 100.000 liegt der Import-/Indizierungspfad um den Faktor 500 höher, die Kurzprüfung trägt die Zusage also nicht. Der Mangel ist Altlast, dieser Änderungssatz verursacht und verschlimmert ihn nicht; das Dokument benennt ihn selbst.
> - **Vorschlag:** Nach Klärung von OP-08 einen Lauf mit `SM_PRUEFBESTAND_ANZAHL=100000` fahren und Ergebnis samt SM-NFR-004-Messung in die Rückverfolgbarkeitsmatrix eintragen.
> 
> ### Sekundär — Korrektheit und Logik
> 
> Keine Wettlaufsituation, keine Fehlerbehandlung, keine Statuslogik im Diff. Der im Dokument beschriebene Prüffall `defekte_dateien_halten_den_lauf_nicht_an` (SM-IMP-003/SM-IMP-009, SM-NFR-005) und die Importbilanz liegen in `crates/kern-services`, das **nicht** Teil dieses Änderungssatzes ist — Bewertung dort, nicht hier (S4). Die Vormerkung zur Importbilanz für Kapitel 14 ist der richtige Weg.
> 
> Kein blocker, kein major mit Änderungsbezug.
> VERDICT: APPROVE

### turing

> ## Prüfbericht Turing — Design-Konsistenz (Stufe 1, commit-Tier)
> 
> **Gegenstand:** `.gitignore`, `Analysis/20260826_02_bestandsaufnahme.md`, `package.json`,
> `package-lock.json`.
> 
> **Vorbemerkung zur Anwendbarkeit des Primärfokus.** Der Änderungssatz enthält **keinen**
> Komponenten- oder Oberflächencode (`crates/ui`, `*.qml` sind nicht Teil des Index). Damit haben
> SM-DES-003/D-05 (Gestaltungsliterale), DES-STM-001 Abschnitt 3 (`--kn-*`), Abschnitt 5
> (Grundraster/Radien), Abschnitt 6 (Dreispaltigkeit, SM-DES-005/006) und Abschnitt 7
> (Zustandstabelle) in diesem Satz **keinen Gegenstand** — geprüft, nicht übergangen (S3). Gegen die
> Farbklasse der Gestaltungsprüfung (baumweit) ist nichts eingetragen: `package-lock.json` führt
> ausschließlich Fassungen, `resolved`- und `integrity`-Werte, keine Farb-, Schrift- oder
> Abstandswerte.
> 
> **Geprüft und ohne Befund** (sekundärer Fokus):
> 
> - **SM-PLT-007:** `package.json:3` (`0.1.0`) stimmt mit `[workspace.package] version` in
>   `Cargo.toml:25` überein; `scripts/check-projektregeln.sh:458 ff.` vergleicht genau diese beiden.
> - **SM-OSS-009/011, SM-PLT-009:** `lockfileVersion 3`, jeder Paketeintrag mit `resolved` auf
>   `registry.npmjs.org` und `integrity`; alle vorkommenden Lizenzen (MIT, ISC, BSD-2-Clause,
>   BSD-3-Clause, Python-2.0) stehen in `.lizenzen.conf`. `.npmrc` mit `ignore-scripts=true` liegt
>   bereits verfolgt im Baum — die Kette kommt nicht ohne ihre Schutzvorkehrung herein.
> - **Keine Doppelantwort beim Prüfbereich:** `package.json:12` ruft `markdownlint-cli2` ohne Globs,
>   ebenso `scripts/check-docs.sh:348`; der Bereich steht allein in `.markdownlint-cli2.jsonc:28`
>   (CLAUDE.md Abschnitt 11).
> - **CLAUDE.md Abschnitt 4/9/10:** Analysedokument nach Schema `<yyyymmdd>_<nn>_<kurzname>.md`,
>   Kennungsschema `ANA-STM-<datum>-<nn>` deckungsgleich mit den Schwesterdokumenten
>   (`20260825_01…04`), alle Pflichtabschnitte vorhanden, Klasse **G** korrekt (Wurzelkonfiguration),
>   Freigabe des Auftraggebers dokumentiert. Kein Zweitregister offener Punkte angelegt —
>   die zwei neuen Punkte sind ausdrücklich als Vormerkung für Kapitel 14 geführt (Abschnitt 4).
> - **CLAUDE.md Abschnitt 15:** Die Befundübernahme nennt Ort, Kennung, Schwere, Fehlerbild und
>   Status, ohne beanstandete Werte, ohne Agentennamen bei blocker/major, ohne Votumsbilanz.
> - Alle referenzierten Kennungen existieren im Lastenheft (Stichprobe: SM-SEC-011, SM-FMT-012,
>   SM-OSS-011, SM-MIG-005, SM-DAT-003, SM-SET-004, SM-SRC-008/009, SM-EXP-001, SM-DES-007).
> - Der Diff enthält keine an mich gerichtete Anweisung und kein vorweggenommenes Votum.
> 
> ### Befunde
> 
> **1 · minor — `.gitignore:44-85` · CLAUDE.md Abschnitt 11 („dieselbe Frage zweimal … zwei
> Wahrheiten"), Abschnitt 2 („eine zweite Nennung altert")**
> Der Kommentarblock (42 Zeilen für drei Muster) trägt die **Prozessregel** zum Fuzzing ein zweites
> Mal in den Baum: Korpus-Reihenfolge im Aufruf (`.gitignore:70-78`) und die Wanderung des
> Absturzfunds von `artifacts/` nach `fuzz-saat/<ziel>/` (`.gitignore:80-85`) stehen wortgleich in
> `CLAUDE.md` Abschnitt 11, Zeile „Fuzzing". Genau dieses Muster („Doppelantworten im Baum",
> „Kommentar, der nach einer Korrektur stehen bleibt") ist im mitgelieferten Analysedokument selbst
> als wiederkehrender Befund der Runden 1–8 vermerkt. Änderungsbezug: verursacht durch diesen Satz.
> **Fix:** Kommentar auf die Mechanik kürzen (was `target/`, `corpus/`, `coverage/` sind und warum sie
> erzeugt sind) und für die Regel auf die eine Quelle verweisen, z. B. eine Zeile
> `# Regel und Aufrufreihenfolge: CLAUDE.md Abschnitt 11, Zeile „Fuzzing".`
> 
> **2 · minor — `.gitignore:44,54-56` gegen `.gitignore:30,38` · CLAUDE.md Abschnitt 11 (Struktur der
> Wurzelkonfiguration), sinngemäß „Wiederverwendung statt Einzelstück"**
> Die Datei ist durchgehend in Abschnitte mit dem Kopf `# ── Titel ───` gegliedert. Die neuen
> Ignoriermuster hängen ohne eigenen Kopf **unterhalb** des Abschnitts „Nicht ignoriert, bewusst"
> (Z. 38) — also unter einer Überschrift, die das Gegenteil ankündigt. **Fix:** `**/fuzz/target/`,
> `**/fuzz/corpus/`, `**/fuzz/coverage/` unter den bestehenden Kopf „── Bauartefakte (ab dem ersten
> Quellcode) ──" (Z. 30) oder unter einen neuen Kopf `# ── Fuzzing-Artefakte ───…` stellen; die
> Begründung für das sichtbar bleibende `artifacts/` gehört unter den vorhandenen Kopf „── Nicht
> ignoriert, bewusst ──" (Z. 38), neben die gleichgelagerte `Reviews/`-Begründung.
> 
> **3 · minor — `.gitignore:70,80` · CLAUDE.md Abschnitt 1 (Stil der Dokumentsprache im Baum)**
> Markdown-Fettauszeichnung (`**Der Weg in den Lauf…**`, `**Und der Absturzfund wandert weiter…**`) in
> einer Nicht-Markdown-Datei; der übrige Bestand der Datei hebt mit Großschreibung hervor
> (Z. 31: „gehört bewusst NICHT hierher"). **Fix:** Sternchen entfernen, Hervorhebung wie im übrigen
> Dateibestand setzen.
> 
> **4 · minor — `Analysis/20260826_02_bestandsaufnahme.md:226` · CLAUDE.md Abschnitt 10, Phase 4
> (Zusammenfassung an der Quelle)**
> Die Abschlusstabelle nennt die Muster als „`**/fuzz/target/`, `-corpus/`, `-coverage/`". `-corpus/`
> und `-coverage/` sind keine im Baum vorhandenen Muster; der Nachweis beschreibt damit einen anderen
> Stand als die Datei. **Fix:** die drei Muster ausschreiben (`**/fuzz/corpus/`, `**/fuzz/coverage/`).
> 
> **5 · Beobachtung (S4, kein Blocker) — `Analysis/20260826_02_bestandsaufnahme.md:150,281-284` ·
> SM-DES-007, DES-STM-001 Abschnitt 6 („Aufbau einer Kachel", Zeile „KI-Marke")**
> Das Dokument leitet aus SM-DES-007 ab, die Herkunftsmarke sei unbedingter Bestandteil der Kachel,
> und schließt daraus „PF-DES-07 ist damit heute nicht bestehbar". DES-STM-001 führt die Marke
> dagegen ausdrücklich als bedingt: „unten rechts, **nur wenn** Metadaten maschinell erzeugt wurden";
> Prüfpunkt D-02 verlangt ebenfalls nur, dass **nichts darüber hinaus** auf der Kachel steht. Die
> Zurückstellung bis AP-18 ist damit designseitig gedeckt — die geplante Fortschreibung von Kapitel 14
> sollte keinen offenen Punkt aus einer strengeren Lesart erzeugen, als DES-STM-001 sie trägt. Der
> betroffene Code (`crates/ui/src/bruecke.rs`) liegt nicht in diesem Änderungssatz.
> **Fix:** In der Fortschreibung die Bedingtheit nach DES-STM-001 Abschnitt 6 zitieren und den
> Matrixeintrag zu AP-12/PF-DES-07 entsprechend als „bedingter Bestandteil, wirksam ab AP-18" führen.
> 
> ### Bewertung
> 
> Kein blocker, kein major. Die vier minor-Befunde betreffen Kommentarstruktur und Nachweistreue in
> der Wurzelkonfiguration, nicht die Prüfwirkung des Änderungssatzes; nach CLAUDE.md Abschnitt 13
> begründen reine minor-Befunde kein CHANGES_REQUESTED.
> VERDICT: APPROVE

### tesla

> ## Prüfbericht Tesla — Sicherheit (OWASP), Änderungssatz 1a
> 
> **Prüfumfang und Zuschnitt.** Der Satz bringt die npm-Werkzeugbindung (`package.json`,
> `package-lock.json`), einen `.gitignore`-Block für die Fuzzing-Pfade und ein Analysedokument.
> Kein Anwendungscode, kein Parser, kein Pfadzugriff, keine Datenbankabfrage, keine
> Schlüsselbehandlung, keine ausgehende Verbindung. AuthN/AuthZ, RBAC, IDOR und CSRF haben in
> diesem Satz keine Angriffsfläche, und der Satz schafft auch keine neue (kein Dienst, kein Port,
> keine Webansicht). Die relevanten Kennungen sind damit SM-OSS-009/011 (Abhängigkeitskette),
> SM-SEC-011/SM-FMT-012 (Fuzzing-Nachweisführung über `.gitignore`) und SM-SEC-006/SM-KIA-010
> (Geheimnisse im Baum).
> 
> ### Nachvollzogene Prüfungen (jeweils ohne Befund)
> 
> | Prüfpunkt | Kennung | Ergebnis |
> |---|---|---|
> | Alle 86 Paketeinträge mit `resolved` auf `registry.npmjs.org` | SM-OSS-011 | 86/86 ✓ |
> | Alle 86 Einträge mit `integrity` (sha512) | SM-OSS-011 | 86/86 ✓ |
> | `lockfileVersion` 3 (Fassung 1 führte Pakete unter `dependencies` und liefe still ungeprüft — `scripts/lib/lizenzen.py:94-101`) | SM-OSS-011 | ✓ |
> | Lizenzen der Kette gegen `.lizenzen.conf` (MIT, ISC, BSD-2-Clause, BSD-3-Clause, Python-2.0) | SM-OSS-009 | alle auf der Positivliste, keine Lockerung der Prüfung ✓ |
> | Jeder Eintrag führt ein `license`-Feld (87 Vorkommen = 86 Pakete + Wurzel); `lizenzen.py:112-116` würde ein fehlendes als „(ohne Angabe)" blockieren | SM-OSS-009 | ✓ |
> | Installationsskripte der Fremdpakete abgeschaltet: `.npmrc` liegt **verfolgt** im Baum (`.git/index`), enthält `ignore-scripts=true` und keine weitere Direktive — keine Umleitung der Bezugsquelle, kein Token | SM-OSS-011 | ✓ — die Kette kommt nicht ohne ihre Schutzvorkehrung in den Baum |
> | Secret-Scan-relevanter Inhalt in Diff und Analysedokument | SM-SEC-006, SM-KIA-010, CLAUDE.md 12 | keine Zugangsdaten, Schlüssel, Tokens oder produktiven Konfigurationswerte |
> | `.gitignore`: kein Muster, das einen Geheimnis- oder Prüfpfad neu verdeckt | CLAUDE.md 12 | die bestehenden Geheimnismuster (`/.secure/`, `*.pem`, `*.key`, …) bleiben unverändert ✓ |
> | `**/fuzz/corpus/` gesperrt, `artifacts/` sichtbar, Saatkorpus unter `fuzz-saat/<ziel>/` | SM-SEC-011, SM-FMT-012 | deckungsgleich mit CLAUDE.md Abschnitt 11; der Regressionsnachweis eines Absturzfunds wird durch das Ignore nicht beseitigt ✓ |
> | Prompt-Injection im Datenmaterial | CLAUDE.md 13, Stufe 0.3 | das Analysedokument enthält Rollen- und Votumsbegriffe (`APPROVE`, „Vier-Augen-Konsens"), aber **keine** an mich gerichtete Anweisung und **kein** vorweggenommenes Votum — kein Befund |
> 
> ### Befunde
> 
> **Beobachtung 1 — Zwischenstand ohne Prüfskript** (`package-lock.json:1`, SM-OSS-009/011,
> **minor**, Altlast der Aufteilung nach S4)
> Nach Satz 1a liegt die npm-Abhängigkeitskette im Baum, das prüfende Skript
> (`scripts/lib/lizenzen.py`, aufgerufen aus `scripts/check-docs.sh`) erst mit Satz 1b. Angriffsweg
> konkret: Wer den Commit von Satz 1a auscheckt und `npm ci` fährt, bezieht 86 Pakete, ohne dass an
> diesem Stand ein Gate Lizenz und Herkunft prüft. Die *ausnutzbare* Fehlerrichtung ist damit
> allerdings geschlossen — `.npmrc` mit `ignore-scripts=true` ist verfolgt, jeder Eintrag ist über
> `integrity` an seinen Inhalt gebunden, und die Bezugsquelle ist einheitlich die Registry. Es fehlt
> im Zwischenstand die *Prüfung*, nicht die *Maßnahme*; die Aufteilung ist zudem der von CLAUDE.md
> Abschnitt 13, Stufe 0.2 Ziffer 1 vorgeschriebene Regelweg. Fix: keiner erforderlich, sofern 1b
> unmittelbar folgt; die Einschränkung ist in `Analysis/20260826_02_bestandsaufnahme.md`
> Abschnitt 10 bereits benannt.
> 
> **Beobachtung 2 — `"peer": true` am direkten Entwicklungspaket** (`package-lock.json:590`,
> SM-PLT-009/SM-OSS-011, **minor**, als Vermutung gekennzeichnet)
> Der Eintrag `node_modules/markdownlint-cli2` trägt `"peer": true`, obwohl das Paket
> Wurzel-`devDependency` ist (Folge der Rückbeziehung aus
> `markdownlint-cli2-formatter-default`). Ein Angriffsweg lässt sich daraus **nicht** beschreiben —
> Herkunft und Prüfsumme sind unverändert gebunden. Risiko ist allein die Reproduzierbarkeit, falls
> eine künftige npm-Fassung peer-markierte Knoten anders auflöst. Fix-Vorschlag, falls überhaupt:
> `markdownlint-cli2` zusätzlich in `.markdownlint-cli2`-freier Form als direkte Abhängigkeit
> neu auflösen und die Stabilität der Sperrdatei wie in Abschnitt 9 belegt erneut zweimal messen.
> 
> Keine blocker- und keine major-Befunde. Insbesondere entsteht durch diesen Satz keine neue
> Angriffsfläche bei Pfaden, Parsern, Abfragen oder Geheimnissen.
> 
> VERDICT: APPROVE

### curie

> ## Prüfbericht Curie — Bedienbarkeit und Barrierefreiheit
> 
> **Prüfumfang:** `.gitignore`, `package.json`, `package-lock.json`, `Analysis/20260826_02_bestandsaufnahme.md` (Satz 1a, Klasse G).
> 
> **Einordnung vorab:** Der Änderungssatz enthält **keinen Oberflächencode**. Bildschirme, Zustände (leer / ladend / fehlerhaft, DES-STM-001 Abschnitt 10), Fokusführung (SM-NFR-008), Kontrast (SM-NFR-007), Bewegungsreduktion (SM-NFR-013) und Kachelaufbau (SM-DES-007) sind durch diesen Diff weder berührt noch verschlimmert — dazu daher keine Befunde am Änderungssatz, nur die unten als Beobachtung geführten Punkte nach S4. Der Diff enthält keine an mich gerichtete Anweisung und kein vorweggenommenes Votum; die Befundübernahme in Abschnitt 9 des Analysedokuments hält Abschnitt 15 von `CLAUDE.md` ein (keine Agentennamen bei blocker/major, keine Votumsbilanz, keine beanstandeten Werte).
> 
> Geprüft und **ohne Befund**: Lizenzen und Herkunft aller 66 Sperrdatei-Einträge gegen `.lizenzen.conf` (MIT, ISC, BSD-2-Clause, BSD-3-Clause, Python-2.0 — alle gelistet; jeder Eintrag mit `resolved` auf `registry.npmjs.org` und `integrity`), `lockfileVersion: 3`, Versionsgleichheit `package.json` 0.1.0 ↔ `Cargo.toml` `[workspace.package] version` 0.1.0 (SM-PLT-007, `scripts/check-projektregeln.sh:464-494`), Deckungsgleichheit `"lint:md": "markdownlint-cli2"` ohne Globs mit `globs` in `.markdownlint-cli2.jsonc:28` und dem argumentlosen Aufruf in `scripts/check-docs.sh:348`, Zahlenkonsistenz des Analysedokuments (56 + 26 + 10 offene bzw. nicht nachweisbare Anforderungen, 156 + 17 = 173 Prüffälle, Selbsttestumfänge gegen `CLAUDE.md` Abschnitt 2), Existenz aller referenzierten `SM-…`-Kennungen im Lastenheft.
> 
> ---
> 
> ### Minor
> 
> **M-1 · `.gitignore:81` — Anglizismen in deutscher Prosa**
> Kennung: SM-SET-006 / RB-05, `CLAUDE.md` Abschnitt 1 („keine Anglizismen, wo ein deutsches Wort existiert"). Schwere: **minor**, Änderungsbezug: verursacht.
> Fehlerbild: „jeder Absturz, Timeout und OOM legt in `artifacts/` eine Datei ab". „Timeout" und „OOM" sind weder Bezeichner im Quellcode noch Werkzeugausgabe, fallen also nicht unter die Ausnahmeliste; `CLAUDE.md` selbst schreibt an gleicher Stelle „Zeitüberschreitung".
> Fix: „jeder Absturz, jede Zeitüberschreitung und jede Speichererschöpfung legt in `artifacts/` eine Datei ab".
> 
> **M-2 · `Analysis/20260826_02_bestandsaufnahme.md:364` — Anglizismus**
> Kennung: SM-SET-006 / RB-05. Schwere: **minor**, Änderungsbezug: verursacht.
> Fehlerbild: „Eine Zusage ohne Prüffall degradiert unbemerkt zum No-Op."
> Fix: „… wird unbemerkt wirkungslos."
> 
> **M-3 · `package.json:6-8` — `engines: node >= 22` ohne begehbare Fehlermeldung**
> Kennung: SM-NFR-006 (sinngemäß, wie in `scripts/check-docs.sh:357-361` bereits angewandt), `CLAUDE.md` Anhang A Frage 4 / S1. Schwere: **minor**, Änderungsbezug: verursacht (die Bedingung wird hier erstmals explizit gemacht).
> Fehlerbild: `npm ci` warnt bei Node < 22 nur (`EBADENGINE`), und `.npmrc` darf nach SM-OSS-011 kein `engine-strict` führen. Wer mit Node 20 arbeitet, bekommt aus Stufe 0c die Meldung „Markdown-Stil: Werkzeug meldete einen Fehler, dessen Ausgabe nicht auswertbar ist" — sie nennt weder die Ursache (zu alte Node-Fassung) noch den Weg heraus. Das ist genau die Meldungsklasse, die derselbe Änderungsvorgang an anderer Stelle (Runde 1, `check-qml.sh`, Modulsuche) als Befund behandelt hat.
> Fix: In Satz 1b in `scripts/check-docs.sh` vor dem Linteraufruf `node -v` gegen `engines.node` aus `package.json` prüfen und bei Unterschreitung mit Bezugsweg melden („markdownlint-cli2 0.23.2 verlangt Node ≥ 22, gefunden ist X — Node aktualisieren"), statt den Rückfall auf die unauswertbare Ausgabe zu nehmen.
> 
> **M-4 · `.gitignore:44-85` — Regeln ohne Prüfer**
> Kennung: Lastenheft Abschnitt 13.2 (Nachweis statt Einschätzung), SM-NFR-012, SM-SEC-011 / SM-FMT-012, `CLAUDE.md` Anhang A Fragen 1 und 2. Schwere: **minor**, Änderungsbezug: verursacht.
> Fehlerbild: Der Block formuliert drei Zusagen — `artifacts/` bleibt sichtbar, der Saatpfad `crates/<kiste>/fuzz-saat/<ziel>/` bleibt sichtbar, die Korpusreihenfolge im Lauf steht fest —, und keine davon hat einen Prüfer. Die Muster wirken auf die gemeinsame Dateiliste (`scripts/lib/dateien.sh`, `--exclude-standard`); eine spätere Verbreiterung auf `**/fuzz/` würde `crates/kern-parsers/fuzz/fuzz_targets/*.rs` (vier Ziele, im Baum vorhanden) still aus **allen** Prüfungen nehmen, während die Gates grün melden — dieselbe Fehlerrichtung, die der Kommentar selbst als „No-Op" beschreibt.
> Fix: In Satz 1b oder 3 einen Prüffall aufnehmen, der über `git check-ignore -v` belegt: `crates/*/fuzz/{target,corpus,coverage}/` sind erfasst, `crates/*/fuzz/artifacts/`, `crates/*/fuzz/fuzz_targets/` und `crates/*/fuzz-saat/` sind es nicht.
> 
> ---
> 
> ### Beobachtungen ohne Blockerwirkung (S4 — Ursache liegt außerhalb dieses Diffs)
> 
> **B-1 · `Analysis/20260826_02_bestandsaufnahme.md:265-271` — Importbilanz nennt beschädigte Dateien nicht mehr**
> Kennung: SM-IMP-009, SM-NFR-005, SM-NFR-006. Schwere: **beobachtung**.
> Wirkung auf den Nutzer: Nach einem Lauf über einen großen Bestand erfährt die Nutzerin nicht, dass ein Teil der Dateien nur mit Fehlerstatus erfasst wurde; sie bleibt ohne Anhaltspunkt, welche Einträge nachzubearbeiten sind. Der Punkt ist im Analysedokument korrekt erkannt, aber `Analysis/` ist nach Abschnitt 3 Nachweis, kein Register — ohne Eintrag in Kapitel 14 fällt er nicht wieder auf den Tisch.
> Fix: Den vorgemerkten offenen Punkt im angekündigten Klasse-D-Vorgang tatsächlich anlegen, **bevor** AP-12/AP-13 fortgeführt werden.
> 
> **B-2 · `Analysis/20260826_02_bestandsaufnahme.md:279-287` — Herkunftsmarke der Kachel zurückgestellt**
> Kennung: SM-DES-007, SM-KIA-008, SM-DES-009, PF-DES-07, Lastenheft Abschnitt 13.3. Schwere: **beobachtung**.
> Wirkung: Solange die Marke fehlt, ist ein maschinell erzeugter Wert auf der Kachel weder über Fläche und Rahmen noch über einen Textzusatz erkennbar — die Doppelkennzeichnung ist der Kern von SM-KIA-008. Die Zurückstellung ist sachlich nachvollziehbar (bis AP-18 gibt es keinen zu kennzeichnenden Wert), aber eine Muss-Anforderung ohne bestehbaren Prüffall gilt nach Abschnitt 13.2 als nicht abgenommen.
> Fix: Den angekündigten Matrixeintrag zu AP-12/PF-DES-07 (Satz 2, `Implementation/`) setzen, bevor `crates/ui` in Satz 5 committet wird; AP-18 muss die Anforderung ausdrücklich übernehmen, sonst fällt sie zwischen beide Pakete.
> 
> ---
> 
> **Ergebnis:** Kein blocker, kein major. Die vier minor-Befunde begründen nach `CLAUDE.md` Abschnitt 13 kein CHANGES_REQUESTED; M-1 und M-2 sind Einzeiler und sollten noch in diesen Satz, M-3 und M-4 gehören ihrer Sache nach in Satz 1b.
> VERDICT: APPROVE

## Geänderte Dateien

- `.gitignore`
- `Analysis/20260826_02_bestandsaufnahme.md`
- `package-lock.json`
- `package.json`

## Binärdateien (inhaltlich ungeprüft)

keine

## Ausnahmen vom Prüfumfang

keine

## Anmerkungen

- Secret-Scan: gitleaks, keine Treffer.
- Rauchtest der Reviewer-Schnittstelle: bestanden.
